export enum AppUrl {
  home = "/",
  login = "/login",
  article = "/articles/[id]",
}

export enum AdminUrl {
  index = "/admin",
  createArticle = "/admin/create-article",
  editArticle = "/admin/edit-article",
}
