import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login",
};

export default function Page() {
  return (
    <div>
      <h1 className="text-center text-4xl text-nb-blue">login</h1>
    </div>
  );
}
