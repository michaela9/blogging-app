import { render } from "@testing-library/react";
// import RecentArticles from '@/containers/recentArticles/'

it("should true is true", () => {
  const condition = true;
  // render()
  console.log(render);
});
